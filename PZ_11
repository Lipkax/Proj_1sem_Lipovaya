# 1. Средствами языка Python сформировать текстовый файл (.txt), содержащий
# последовательность из целых положительных и отрицательных чисел. Сформировать
# новый текстовый файл (.txt) следующего вида, предварительно выполнив требуемую
# обработку элементов:
# Исходные данные:
# Количество элементов:
# Индекс первого максимального элемента:
# Произведение элементов средней трети:
l = ['-44 2 12 -69 1 67 120 -10 17']  # Запишем в файл data_1.txt список
f1 = open('data_1.txt', 'w')
f1.writelines(l)
f1.close()
f2 = open('data_2.txt', 'w')  # Дублируем список в новый файл data_2txt
f2.write('Исходные данные: ')
f2.write('\n')
f2.writelines(l)
f2.close()
f1 = open('data_1.txt')  # преобразуем строку в числа
k = f1.read()
k = k.split()
for i in range(len(k)):
    k[i] = int(k[i])
f1.close()
f1 = open('data_1.txt')  # Ищем максимальный элемент и количество элементов в файле data_1.txt и записываем в файл data_2.txt
max, t = 0, 0
for i in range(len(k)):
    max = max if max > k[i] else k[i]
if k[i] < 0:
    t += 1
f2 = open('data_2.txt', 'a')  # открываем файл для дозаписи
f2.write('\n')
print('Количество элементов: ', len(k), '\nИндекс первого максимального числа: ', max, file=f2)
f2.close()
f1 = open('data_1.txt')
test = f1.read()
print(test)
